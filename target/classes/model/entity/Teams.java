package model.entity;

/**
 * An enum containing teams by which to identify other entities
 * on the same or opposing team - currently, only players and enemies
 */
public enum Teams {
    PLAYER,
    ENEMY
}
